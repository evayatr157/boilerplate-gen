# Python Django PostgreSQL Starter Kit

## Setup

1. Install the setup script:

```
npm install
```

2. Run the setup script:

```
npm run setup
```

This will create a `.env` file and install necessary Python dependencies.

## Run the Application

Use Docker Compose to run the application:

```
docker-compose up
```

This will start the FastAPI application along with a PostgreSQL database instance.